// main.js

// Espera a que el contenido esté listo antes de inicializar
document.addEventListener("DOMContentLoaded", () => {
    renderProductos(); // Renderizar productos
    actualizarCarrito(); // Renderizar carrito con datos de localStorage
});
